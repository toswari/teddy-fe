## Project Overview

This is a Python-based image classification project that uses the Clarifai API to analyze images through a workflow. The project consists of a single script that demonstrates image classification capabilities.

## Code Structure

- `Comparison_OpenTable.py`: Main script that connects to Clarifai's image classification workflow and processes images
- `Images/`: Directory containing test image sets (note: contains sensitive/inappropriate content for testing purposes)
