version: '3.8'

services:
  clarifai-video-processor:
    build: .
    ports:
      - "8081:8080"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    container_name: clarifai-video-processor