version: '3.8'

services:
  clarifai-baseline-runner-docker:
    container_name: clarifai-baseline-runner-docker
    build: .
    ports:
      - "8081:8080"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped